#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Seccess/correct)
 */
int main(void)

{
char alphabet;

for (alphabet = 'a'; alphabet <= '2'; alphabet++)
{
putchar(alphabet);
}

putchar('\n');

return (0);
}
